# Evo-1 与 LeRobot 快速参考指南

> 5 分钟速查手册

---

## 🎯 核心概念速查

### Evo-1 是什么？

**一句话**：轻量级视觉-语言-动作模型，让机器人根据图像和语言指令学会操作任务。

**关键数字**：
- 0.77B 参数（竞品：3.5B-7B）
- 2.3 GB GPU 内存（竞品：10-18 GB）
- 16.4 Hz 推理速度
- 80.6% Meta-World 成功率（竞品最佳：68.2%）

### LeRobot 是什么？

**一句话**：机器人学习的完整工作流平台，从数据收集到模型部署的一站式解决方案。

**核心功能**：
- 统一数据格式（HuggingFace 兼容）
- 多种算法（ACT, Diffusion, Evo-1...）
- 硬件支持（SO-100, xArm6, ALOHA...）
- CLI 工具链（record, train, eval）

---

## 📊 对比速查表

| 特性 | Evo-1 | LeRobot |
|------|-------|---------|
| **类型** | 单一模型 | 框架平台 |
| **参数量** | 0.77B | 取决于选择的策略 |
| **GPU 需求** | 2.3 GB | 2-18 GB (因策略而异) |
| **数据需求** | 50-100 演示/任务 | 通常更多 |
| **语言理解** | ✅ 内置 (InternVL3) | 依策略而定 |
| **学习曲线** | 中等 | 陡峭 |
| **适用场景** | 快速部署、资源受限 | 研究、实验、社区协作 |

---

## 🏗️ Evo-1 架构速记

```
输入: [图像1, 图像2] + "任务描述" + 机器人状态
  ↓
[层1] InternVL3-1B: 视觉-语言理解
  ↓
[层2] 信息整合: 融合多模态特征 + 状态
  ↓
[层3] Flow Matching: 从噪声迭代 50 步到精确动作
  ↓
输出: 未来 50 步动作 [50 × 动作维度]
```

### 训练流程速记

```
Stage 1 (5000 步):
  - 冻结 VLM
  - 只训练动作头
  - 目标：学会利用语义特征

Stage 2 (80000 步):
  - 解冻所有层
  - 端到端微调
  - 目标：整体优化
```

---

## 🛠️ 常用命令速查

### Evo-1 独立使用

```bash
# 1. 环境安装
conda create -n Evo1 python=3.10
conda activate Evo1
cd Evo_1
pip install -r requirements.txt
MAX_JOBS=64 pip install -v flash-attn --no-build-isolation

# 2. 下载预训练模型
huggingface-cli download MINT-SJTU/Evo1_MetaWorld \
  --local-dir ./checkpoints/

# 3. 运行推理（Meta-World）
# Terminal 1: 启动模型服务器
python scripts/Evo1_server.py

# Terminal 2: 运行评估客户端
cd ../MetaWorld_evaluation
python mt50_evo1_client_prompt.py

# 4. 训练自己的模型
# Stage 1
accelerate launch scripts/train.py \
  --run_name Evo1_stage1 \
  --action_head flowmatching \
  --lr 1e-5 \
  --batch_size 16 \
  --max_steps 5000 \
  --finetune_action_head \
  --save_dir ./checkpoints/stage1

# Stage 2
accelerate launch scripts/train.py \
  --run_name Evo1_stage2 \
  --action_head flowmatching \
  --lr 1e-5 \
  --batch_size 16 \
  --max_steps 80000 \
  --finetune_vlm \
  --finetune_action_head \
  --resume --resume_pretrain \
  --resume_path ./checkpoints/stage1/step_5000 \
  --save_dir ./checkpoints/stage2
```

### LeRobot 使用

```bash
# 1. 安装 LeRobot
conda create -n lerobot python=3.10
conda activate lerobot
git clone https://github.com/huggingface/lerobot
cd lerobot
pip install -e .

# 2. 数据收集
lerobot-record \
  --robot.type=so100 \
  --robot.port=/dev/ttyACM0 \
  --task="抓取红色方块" \
  --episodes=50

# 3. 数据可视化
lerobot-visualize --dataset=data/my_task

# 4. 训练模型（使用 Evo-1）
lerobot-train \
  --policy=evo1 \
  --dataset=data/my_task \
  --num_epochs=100

# 5. 评估模型
lerobot-eval \
  --policy=checkpoints/evo1_my_task \
  --num_episodes=10

# 6. 实际部署
lerobot-record \
  --robot.type=so100 \
  --policy=checkpoints/evo1_my_task \
  --task="抓取红色方块"
```

### Evo-1 在 LeRobot 中使用（SO-100）

```bash
# 1. 准备环境
cd Evo_1/so100_evo1/lerobot-main
conda create -n Evo1_SO100 python=3.10
conda activate Evo1_SO100

# 2. 安装依赖
pip install flash_attn-2.8.3+cu12torch2.7cxx11abiTRUE-cp310-cp310-linux_x86_64.whl
conda install ffmpeg -c conda-forge
pip install -e .
pip install -e ".[feetech]"

# 3. 设置环境变量
export HF_LEROBOT_HOME="/path/to/your/lerobot_home"

# 4. 下载预训练模型
huggingface-cli download MINT-SJTU/Evo1_SO100 \
  --local-dir ./checkpoints/evo1_so100

# 5. 运行推理
lerobot-record \
  --robot.type=so100_follower \
  --robot.port=/dev/ttyACM0 \
  --robot.cameras="{
    front: {type: opencv, index_or_path: 2, width: 640, height: 480, fps: 30},
    wrist: {type: opencv, index_or_path: 0, width: 640, height: 480, fps: 30}
  }" \
  --policy.path=./checkpoints/evo1_so100 \
  --dataset.single_task="抓取绿色立方体"
```

---

## 💡 代码示例速查

### Evo-1 推理代码

```python
from Evo_1.scripts.Evo1 import EVO1
from PIL import Image
import torch

# 1. 初始化模型
config = {
    "vlm_name": "OpenGVLab/InternVL3-1B",
    "action_head": "flowmatching",
    "horizon": 50,
    "per_action_dim": 7,
    "device": "cuda"
}
model = EVO1(config)
model.load_state_dict(torch.load("checkpoint.pth"))
model.eval()

# 2. 准备输入
images = [
    Image.open("camera1.jpg"),  # 基座相机
    Image.open("camera2.jpg")   # 手腕相机
]
image_mask = torch.tensor([1, 1, 0])  # 前两个有效
state = [0.0, 0.5, -0.3, 0.2, 0.1, 0.0, 1.0]  # 机器人状态
prompt = "抓取红色方块"

# 3. 推理
action_chunk = model.run_inference(
    images=images,
    image_mask=image_mask,
    prompt=prompt,
    state_input=state
)

# 4. 使用动作
# action_chunk shape: [1, 1200] = [1, 50*24]
action_chunk = action_chunk.view(50, 24)
first_action = action_chunk[0]  # 执行第一步
```

### LeRobot 数据集使用

```python
from lerobot.datasets import LeRobotDataset
from torch.utils.data import DataLoader

# 1. 加载数据集
dataset = LeRobotDataset("lerobot/aloha_mobile_cabinet")

print(f"数据集大小: {len(dataset)}")
print(f"Episode 数量: {dataset.num_episodes}")

# 2. 查看单帧数据
frame = dataset[0]
print(frame.keys())
# dict_keys(['observation.images.top', 'observation.state',
#            'action', 'episode_index', 'frame_index'])

# 3. 时序查询（获取动作序列）
temporal_data = dataset.get_temporal_frames(
    index=100,
    delta_timestamps={
        "observation.images.top": [0],      # 当前帧图像
        "action": list(range(16))           # 未来 16 步动作
    }
)
print(temporal_data["action"].shape)  # [16, action_dim]

# 4. 创建 DataLoader
dataloader = DataLoader(
    dataset,
    batch_size=16,
    shuffle=True,
    num_workers=4
)

for batch in dataloader:
    obs = batch["observation"]
    action = batch["action"]
    # 训练模型...
    break
```

### LeRobot 策略使用

```python
from lerobot import Policy

# 1. 从预训练模型加载
policy = Policy.from_pretrained("lerobot/diffusion_pusht")

# 2. 或从本地检查点加载
policy = Policy.from_pretrained("./checkpoints/evo1_my_task")

# 3. 推理
import torch

obs = {
    "images": torch.randn(1, 3, 224, 224),
    "state": torch.randn(1, 7)
}
action = policy.predict(obs)
print(action.shape)  # [1, action_dim]

# 4. 训练
policy.train()
optimizer = torch.optim.Adam(policy.parameters(), lr=1e-4)

for batch in dataloader:
    loss = policy.compute_loss(batch)
    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
```

---

## 🐛 常见问题速查

### Evo-1 常见问题

**Q1: Flash Attention 安装失败**
```bash
# 解决方案1: 减少编译核心数
MAX_JOBS=8 pip install -v flash-attn --no-build-isolation

# 解决方案2: 使用预编译 wheel
wget https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-xxx.whl
pip install flash_attn-xxx.whl
```

**Q2: GPU 内存不足**
```python
# 减小 batch size
--batch_size 8  # 从 16 降到 8

# 使用梯度累积
--gradient_accumulation_steps 2

# 使用混合精度
--mixed_precision fp16
```

**Q3: 推理速度慢**
```python
# 减少 Flow Matching 步数
config["num_inference_timesteps"] = 20  # 从 50 降到 20

# 使用半精度
model.half()

# 启用 CUDA 优化
torch.backends.cudnn.benchmark = True
```

### LeRobot 常见问题

**Q4: 相机索引错误**
```bash
# 查找可用相机
v4l2-ctl --list-devices

# 测试相机
lerobot-camera-test --index 0

# 手动指定
--robot.cameras="{ front: {index_or_path: 2} }"
```

**Q5: 机器人连接失败**
```bash
# 检查设备权限
sudo chmod 666 /dev/ttyACM0

# 查看可用端口
ls /dev/ttyACM*

# 检查波特率
--robot.baudrate=1000000
```

**Q6: 数据集加载慢**
```python
# 设置缓存目录
export LEROBOT_CACHE_DIR="/path/to/fast/ssd"

# 预加载数据集
dataset = LeRobotDataset(
    "my_dataset",
    preload=True  # 全部加载到内存
)
```

---

## 📈 性能优化速查

### Evo-1 优化技巧

```python
# 1. 模型量化（推理加速）
import torch.quantization as quantization

model_int8 = quantization.quantize_dynamic(
    model, {torch.nn.Linear}, dtype=torch.qint8
)

# 2. TorchScript 编译
model_scripted = torch.jit.script(model)
model_scripted.save("model_scripted.pt")

# 3. 批处理推理
images_batch = [images1, images2, images3, ...]
actions = model.run_inference_batch(images_batch)

# 4. 使用 ONNX Runtime
import onnx
torch.onnx.export(model, dummy_input, "model.onnx")
```

### 训练优化技巧

```bash
# 1. 使用 DeepSpeed (多 GPU)
accelerate config  # 选择 DeepSpeed
accelerate launch --config_file deepspeed_config.yaml scripts/train.py

# 2. 梯度检查点（节省内存）
--gradient_checkpointing

# 3. 混合精度训练
--mixed_precision bf16  # 或 fp16

# 4. 优化数据加载
--num_workers 8  # 增加数据加载线程
--prefetch_factor 4
```

---

## 🔬 调试技巧速查

### 打印中间结果

```python
# Evo-1 调试
model = EVO1(config)

# Hook 获取中间特征
def hook_fn(module, input, output):
    print(f"Output shape: {output.shape}")
    print(f"Output mean: {output.mean()}, std: {output.std()}")

model.embedder.register_forward_hook(hook_fn)
model.action_head.register_forward_hook(hook_fn)

# 运行推理
action = model.run_inference(...)
```

### 可视化动作轨迹

```python
import matplotlib.pyplot as plt

# 绘制 Flow Matching 迭代过程
actions_history = []  # 在推理循环中收集

fig, axes = plt.subplots(7, 1, figsize=(12, 10))
for dim in range(7):
    actions_dim = [a[dim] for a in actions_history]
    axes[dim].plot(actions_dim)
    axes[dim].set_title(f"Joint {dim+1}")
plt.tight_layout()
plt.savefig("flow_matching_trajectory.png")
```

### 检查数据集质量

```python
from lerobot.datasets import LeRobotDataset
import matplotlib.pyplot as plt

dataset = LeRobotDataset("my_dataset")

# 1. 检查动作分布
all_actions = []
for i in range(len(dataset)):
    all_actions.append(dataset[i]["action"])

actions_array = torch.stack(all_actions)
print(f"动作均值: {actions_array.mean(dim=0)}")
print(f"动作标准差: {actions_array.std(dim=0)}")

# 2. 可视化一个 episode
episode_idx = 0
episode_data = dataset.get_episode(episode_idx)

for i, frame in enumerate(episode_data):
    img = frame["observation.images.front"]
    plt.imsave(f"episode_{episode_idx}_frame_{i}.png", img)
```

---

## 🎓 学习路径速查

### 初学者（1-2 周）

```
Day 1-2: 概念理解
  □ 观看演示视频
  □ 阅读 README
  □ 理解基本流程

Day 3-4: 环境搭建
  □ 安装 LeRobot
  □ 下载示例数据集
  □ 运行第一个模型

Day 5-7: 实践练习
  □ 在仿真环境测试
  □ 理解数据格式
  □ 尝试可视化工具
```

### 进阶者（2-4 周）

```
Week 1: 深入 Evo-1
  □ 阅读论文 Sections 1-3
  □ 理解 Flow Matching
  □ 复现仿真实验

Week 2: 自定义数据
  □ 收集/生成数据
  □ 转换为 LeRobot 格式
  □ 训练第一个模型

Week 3-4: 模型改进
  □ 调整超参数
  □ 尝试不同配置
  □ 分析失败案例
```

### 研究者（1-3 个月）

```
Month 1: 深入理论
  □ 研读所有相关论文
  □ 理解数学原理
  □ 实现核心算法

Month 2: 实验改进
  □ 设计新架构
  □ 实现改进方案
  □ 系统性评估

Month 3: 贡献开源
  □ 提交 Pull Request
  □ 分享数据集/模型
  □ 撰写技术博客
```

---

## 🔗 资源链接速查

### Evo-1
- 论文: https://arxiv.org/abs/2511.04555
- 代码: https://github.com/MINT-SJTU/Evo-1
- 模型: https://huggingface.co/MINT-SJTU/Evo1_MetaWorld
- 主页: https://mint-sjtu.github.io/Evo-1.io/

### LeRobot
- 代码: https://github.com/huggingface/lerobot
- 文档: https://huggingface.co/docs/lerobot
- 模型库: https://huggingface.co/lerobot
- Discord: https://discord.gg/lerobot

### 背景知识
- Imitation Learning: https://arxiv.org/abs/1811.06711
- Flow Matching: https://arxiv.org/abs/2210.02747
- Vision-Language Models: https://arxiv.org/abs/2304.00685

---

## 📝 速记卡片

### Evo-1 核心公式

```
Flow Matching 训练:
  a_t = (1-t) × noise + t × a_gt
  loss = MSE(v_pred, a_gt - noise)

Flow Matching 推理:
  for i in range(N):
    t = i / N
    v = model(a_t, context, t)
    a_t += dt × v
```

### 关键超参数

```python
# Evo-1 训练
learning_rate = 1e-5
batch_size = 16
horizon = 50
per_action_dim = 7
num_inference_timesteps = 50
grad_clip_norm = 1.0
warmup_steps = 1000

# Stage 1: 5000 steps
# Stage 2: 80000 steps
```

### 性能指标

```
Meta-World: 80.6% (50 tasks)
LIBERO: 94.8% (90 tasks)
RoboTwin: 37.8%
Real Robot: 78% (4 tasks)

推理: 16.4 Hz
GPU 内存: 2.3 GB
训练数据: 50-100 demos/task
```

---

*快速参考指南 v1.0*
*最后更新: 2025-11-19*

💡 **提示**: 将这份文档保存为书签，随时查阅关键命令和代码！
